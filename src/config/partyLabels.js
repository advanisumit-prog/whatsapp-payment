module.exports = {
    // Static label applied to every image from this group.
    // Add one entry per group in your watchlist that needs a fixed label.
    "Moby & kamal vijaywada payment": "Kamal vijaywada",
    "Hr moby": "Hr",
    // Add more groups here as needed...

    // "Ramesh payment" is intentionally left out — it uses DYNAMIC
    // extraction instead (label text that follows each image in-chat).
};